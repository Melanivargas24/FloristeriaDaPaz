@model DaPazWebApp.Models.CategoriaArregloModel

@{
    ViewData["Title"] = "Editar Categoría de Arreglo";
    Layout = "_Layout";
}

<div class="container" style="margin-top:35px; margin-bottom:135px">
    <div class="d-flex justify-between align-items-center encabezado-tablas">
        <h2 class="m-0">Editar Categoría de Arreglo</h2>
    </div>

    <form asp-action="EditarCategoriasA" method="post">
        <input type="hidden" asp-for="idCategoriaArreglo" />
        <div class="form-group">
            <label asp-for="nombreCategoriaArreglo" class="control-label">Arreglo</label>
            <input asp-for="nombreCategoriaArreglo" class="form-control" />
            <span asp-validation-for="nombreCategoriaArreglo" class="text-danger"></span>
        </div>
        <br />
        <div class="d-flex justify-content-end gap-2 mt-4">
            <button type="submit" class="btn-guardar">Guardar</button>
            <a asp-action="ConsultarCategoriasA" class="btn-cancelar">Cancelar</a>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
